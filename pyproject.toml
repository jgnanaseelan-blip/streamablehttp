[project]
name = "weather"
version = "0.1.0"
description = "Weather MCP Server"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "fastmcp>=2.10.1",
    "httpx>=0.28.1",
    "starlette>=0.37.2"
]
