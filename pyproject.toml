[project]
name = "mcpsse"
version = "0.1.0"
description = "Search"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "fastmcp>=2.10.1",
    "httpx>=0.28.1",
    "starlette>=0.37.2",
    "tavily-python>=0.3.0",
    "pytz"
    
]
